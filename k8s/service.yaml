apiVersion: v1
kind: Service
metadata:
  name: player-api
  labels:
    app: player-api
spec:
  type: ClusterIP
  selector:
    app: player-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
